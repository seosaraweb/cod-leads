[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd frontend && npm install && npm run build && cp -r build ../backend/public"]

[start]
cmd = "cd backend && npm start"
